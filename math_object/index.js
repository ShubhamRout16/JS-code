// Math - built in js object which provides a collection of properties and methods


// console.log(Math.PI); for pi
// console.log(Math.E);  for euler's number

let x = 3;
let y = 2;
let z = 1;

// z = Math.round(x); {round off the given number}
// z = Math.floor(x); {gives the floor value means the lower value}
// z = Math.ceil(x); {gives the ceiling value means the upper value}
// z = Math.trunc(x); {short name of truncate which will eliminate the decimal value}
// z = Math.pow(x , y); {raises the value of given to the power}
// z = Math.sqrt(x); {finds the sqr root value of the given value}
// z = Math.log(x); {finds the log value}
// z = Math.sin(x); 
// z = Math.cos(x); 
// z = Math.tan(x); 
//  z = Math.abs(x); {gives the absolute value}
// z = Math.sign(x); {gives the sign of the value}

let max = Math.max(x, y, z);
let min = Math.min(x, y, z);


console.log(min);
